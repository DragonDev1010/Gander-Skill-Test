# Frontend
`frontend` folder is the storage of React application
## Start Application
Run command `yarn start` or `npm start` on the root folder 
## Test Application
Run command `npm test` on the root folder
# Backend
`backend` folder is the storage of Express API
## Start Application
Run command `node server.js` on the root folder